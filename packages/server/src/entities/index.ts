import User from './user.entity';
import UserProfile from './userProfile.entity';
import AuthToken from "./authToken.entity";

const entities: any[] = [User, UserProfile, AuthToken];

export default entities;
